spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sam-mmm/spring-tutorial.git
          search-paths:
            - configurations
          default-label: 10-circuit-breaker-resilience4j-bulkhead
          clone-on-start: true