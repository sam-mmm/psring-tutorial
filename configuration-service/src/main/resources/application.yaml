spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sam-mmm/spring-tutorial.git
          search-paths:
            - configurations
          default-label: 03-basic-setup-for-registry-service
          clone-on-start: true