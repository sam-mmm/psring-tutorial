spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sam-mmm/spring-tutorial.git
          search-paths:
            - configurations
          default-label: 11-resilience4j-circuit-breaker
          clone-on-start: true